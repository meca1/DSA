/**
 * Valid Anagram
 * 
 * Dificultad: Fácil
 * 
 * Descripción:
 * Dadas dos strings `s` y `t`, determina si `t` es un anagrama de `s`.
 * Un anagrama es una palabra formada reordenando las letras de otra.
 * 
 * Ejemplo:
 * Input: s = "anagram", t = "nagaram"
 * Output: true
 * 
 * Input: s = "rat", t = "car"
 * Output: false
 */

function isAnagram(s: string, t: string): boolean {
  // Tu implementación aquí
  return false;
}

// Casos de prueba
console.log(isAnagram("anagram", "nagaram")); // Debería devolver true
console.log(isAnagram("rat", "car"));         // Debería devolver false
console.log(isAnagram("a", "ab"));            // Debería devolver false 