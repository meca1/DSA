/**
 * Product of Array Except Self
 * 
 * Dificultad: Medio
 * 
 * Descripción:
 * Dado un array `nums`, devuelve un array `answer` donde `answer[i]` 
 * es igual al producto de todos los elementos de `nums` excepto `nums[i]`.
 * 
 * Restricción: No puedes usar la operación de división.
 * 
 * Ejemplo:
 * Input: nums = [1,2,3,4]
 * Output: [24,12,8,6]
 * 
 * Input: nums = [-1,1,0,-3,3]
 * Output: [0,0,-9,0,0]
 */

function productExceptSelf(nums: number[]): number[] {
  // Tu implementación aquí
  return [];
}

// Casos de prueba
console.log(productExceptSelf([1, 2, 3, 4]));        // Debería devolver [24, 12, 8, 6]
console.log(productExceptSelf([-1, 1, 0, -3, 3]));   // Debería devolver [0, 0, -9, 0, 0] 