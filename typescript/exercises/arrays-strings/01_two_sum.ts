/**
 * Two Sum
 * 
 * Dificultad: Fácil
 * 
 * Descripción:
 * Dado un array de números enteros `nums` y un entero `target`, 
 * devuelve los índices de los dos números que suman `target`.
 * 
 * Ejemplo:
 * Input: nums = [2,7,11,15], target = 9
 * Output: [0,1] (porque nums[0] + nums[1] == 9)
 */

function twoSum(nums: number[], target: number): number[] {
  // Tu implementación aquí
  return [];
}

// Casos de prueba
console.log(twoSum([2, 7, 11, 15], 9)); // Debería devolver [0, 1]
console.log(twoSum([3, 2, 4], 6));      // Debería devolver [1, 2]
console.log(twoSum([3, 3], 6));         // Debería devolver [0, 1] 